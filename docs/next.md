# 다음에 할 것

지금 잡혀 있지만 아직 손대지 않은 일. 순서는 정해둔 대로다 — 위에서부터 한다.

배포·심사에 걸리는 항목은 여기 두지 않는다. 그건 [docs/store-release.md](store-release.md)와
[docs/security.md](security.md)에 있고, 이 문서는 **기능을 고치는 일**만 모은다.

---

## 0-1. ~~번호가 갈렸다는 사실~~ — 알리지 않기로 했다

번호는 두 군데서 읽힌다 — zxing이 막대를 읽은 값과, 모델이 사진에 인쇄된 숫자를 눈으로
읽은 값. **막대 쪽이 늘 기준이다.**

한때 둘이 갈리면 "사진과 맞는지 확인해주세요"를 띄웠고, 훑기 카드에도 같은 것을 붙였다.
**둘 다 걷어냈다.**

갈렸다는 건 둘 중 하나가 틀렸다는 뜻인데, 틀린 쪽은 거의 항상 인쇄된 숫자다.
막대에는 검산 자리가 있어 안 맞으면 값을 아예 안 내놓는데(CODE_128은 검산 문자,
EAN-13은 체크디지트), 눈으로 읽는 쪽에는 그런 장치가 없다. 6을 5로 읽으면 그냥 5가
된다. 그 눈은 같은 사진의 상품명을 "떠먹는"에서 "더먹는"으로 읽는 눈이기도 하다.

그래서 그 경고는 **맞는 값을 놓고 사람을 세워두는 일이 대부분**이었다. 헛경보가
반복되면 정작 진짜 경고도 안 읽힌다.

훑기에서는 아예 구조적으로 헛경보였다. 후보 자체가 막대에서 읽은 번호로 묶여서
만들어지므로(`client/src/utils/gallery.js`의 `seenCodes`) 인쇄된 숫자는 거기서 어디에도
쓰이지 않는다. 안 쓰는 값이 틀렸다고 알리고 있었던 셈이다.

**인쇄된 숫자가 아직 쓰이는 자리는 한 곳뿐이다** — 직접 올리는 화면에서 막대를 아예
못 읽었을 때(`client/src/utils/imageAnalyze.js`의 `scanned || printed`). 흐릿하거나
막대가 작은 사진이 그렇다. 거기서는 그것 말고 채울 것이 없어서 남겨뒀다.

### 남은 것

- [ ] 등록된 것들 중에 번호가 틀린 게 있는지 한 번 훑어볼 것. 막대를 기준으로 삼기 전에
      등록된 건들은 모델이 읽은 숫자로 들어갔을 수 있다.
- [ ] `client/src/components/BarcodeModal.jsx:73`은 저장된 숫자로 막대를 **새로 그린다**.
      원본에서 잘라둔 바코드 이미지(`barcode_image_path`)가 있는데도 쓰지 않는다. 숫자가
      틀리면 그려진 막대도 틀린다. 크롭을 대신 보여주는 쪽이 나은지 재볼 것 — 사진으로
      찍힌 막대라 스캐너가 못 읽을 수도 있어서, 지금 방식에도 이유는 있다.

---

## 0. 상품명을 한 글자씩 다르게 읽는다

"떠먹는 스트로베리"가 **"더먹는"**으로 나온다. 태수님이 마지막에 잡기로 한 항목이다.

### 지금까지 한 것

압축이 원인인 줄 알았다. 모델이 보던 사진이 보관용으로 눌러둔 사본(1400px, 품질 0.82)
이었고, 한글은 획 하나로 갈리니(떠/따, 반/밤) 압축 자국이 그 위에 앉으면 다른 글자가
된다고 봤다. 실제로 같은 사진이 부를 때마다 "따먹는"과 "뚜렛는"으로 갈렸다.

모델용 사본을 따로 떴다 — 1568px, 품질 0.95 (`client/src/utils/imageAnalyze.js`).
이미지 토큰은 픽셀 수로만 매겨져서 화질을 올려도 요금은 그대로다.

**절반은 맞았다.** "황금올리브 반반"이 제대로 나오게 됐고, 못 읽는 건이 없어졌고,
무엇보다 **여러 번 읽혀도 매번 같은 답**이 나온다. 흔들리던 것이 멎었다.

### 남은 것은 다른 문제다

답이 일정해졌다는 건 이제 노이즈가 아니라는 뜻이다. 모델이 이 해상도에서 그 글자를
진짜로 "더"로 본다. 화질을 더 올려도 같은 답이 나올 것이다.

여기서 쓸 수 있는 수는 두 가지뿐이다.

- [ ] **상품명 자리만 잘라서 크게 보내기.** 지금은 세로로 긴 캡처를 통째로 보내는데,
      화소 상한(115만) 때문에 폭이 700픽셀 언저리로 묶인다. 글자가 있는 부분만 잘라
      보내면 같은 화소로 몇 배 크게 볼 수 있다. 자를 위치를 알아야 하는데, 모델이 이미
      `thumbnail`로 상품 그림 위치를 짚고 있으니 같은 방식으로 상품명 위치도 짚게 하고
      두 번 부르는 식이 된다. 호출이 두 배가 된다.
- [ ] **모델을 올리기.** `claude-haiku-4-5` → `claude-sonnet-5`. 한글 판독은 확실히
      나아지지만 값이 3배다($1/$5 → $3/$15). 한 건 6원이 18원이 된다.

먼저 재볼 것: **같은 사진을 Sonnet에게 한 번 보내보고** 실제로 "떠먹는"이 나오는지.
안 나오면 모델을 올려도 소용없으니 자르는 쪽으로 간다. 두 번 부르는 값(12원)과 모델을
올리는 값(18원)을 비교하는 건 그다음이다.

관련: [scan-cost.md](scan-cost.md)

---

## 1. ~~지도와 외부 연동이 앱에서 안 된다~~ — 됐다

앱은 화면을 **`https://localhost`** 에서 연다. Capacitor의 기본값이고
(`hostname = "localhost"`, `androidScheme = https`), `app/capacitor.config.ts`에
따로 지정한 것이 없어 그대로다. 개발자센터에는 `https://ceaser501.github.io`만
등록돼 있어서 SDK가 401로 떨어졌다.

**등록하는 자리를 찾는 데 가장 오래 걸렸다.** 카카오 콘솔에는 도메인을 적는 칸이
여럿이고 쓰임이 다르다. 다음에 또 헤매지 않도록 적어둔다.

| 자리 | 무엇에 쓰나 |
| --- | --- |
| **앱 → 플랫폼 → Web → 사이트 도메인** | **JavaScript SDK가 보는 유일한 칸** |
| 앱 → 제품 링크 관리 → 웹 도메인 | 카카오톡 링크 공유 미리보기 |
| 앱 → 일반 → 앱 대표 도메인 | 같은 계열. SDK와 무관 |
| 제품 설정 → 카카오 로그인 → Redirect URI | 로그인 후 돌아올 주소 |

- [x] 카카오 **플랫폼 → Web → 사이트 도메인**에 `https://localhost` 추가 → 지도가 뜬다
- [x] 실패 이유를 화면에 남긴다 — `client/src/utils/kakaoMap.js`가 이유를 같이
      돌려주고 `client/src/components/StoreDetailSheet.jsx`가 그대로 보여준다.
      앱에서는 CapacitorHttp로 한 번 더 불러 상태 코드와 본문까지 적는다.
      401을 눈으로 본 덕에 "네트워크냐 도메인이냐"를 한 번에 갈랐다.
- [x] **T맵 길안내** — `intent://` 형태를 풀어주는 것은 크롬이지 웹뷰가 아니다.
      Capacitor는 자기 도메인 밖의 주소를 만나면 `new Intent(ACTION_VIEW, url)`로
      그대로 넘기는데(`Bridge.launchIntent`), 그 url의 scheme이 `intent`라서 받아줄
      앱이 없다. 던져진 `ActivityNotFoundException`은 아무도 잡지 않는다(소스에
      `// TODO`로 남아 있다). 앱 안에서는 `tmap://`를 그대로 넘기고, 대체 주소를
      실을 수 없으니 "1.5초 뒤에도 화면이 그대로면 스토어로"를 같이 쓴다.
- [ ] **아직 안 본 것** — 경로 그리기(자동차·도보)와 주변 매장 검색
      (`supabase/functions/search-places`). 후자는 서버가 부르는 REST라 도메인
      제한과 무관하지만, 지도가 죽어 있어서 확인할 기회가 없었다.

---

## 2. 여러 장을 한 번에 등록하기

지금 `+`는 한 건씩만 받는다. 여러 장을 받아 바코드 번호가 같은 것끼리 묶고,
읽어낸 값을 확인시킨 뒤 한꺼번에 넣는다.

시안 네 가지를 그려뒀다 — <https://claude.ai/code/artifact/696cb219-748e-4d78-b610-58383057bbcb>

| | 전부 잘 읽힌 날 | 한 건 흐릴 때 | 기존 화면과 |
| --- | --- | --- | --- |
| A 묶음 띠 + 편집판 | 5번 눌러야 | 그 건만 고치면 끝 | 새 화면 |
| **B 카드 목록** | **1번** | 그 카드만 펼침 | **같음** |
| C 한 장씩 넘기기 | 5번 넘김 | 넘기다 만남 | 새 화면 |
| **D 빠진 것만 묻기** | **1번** | 그 칸만 물음 | **같음** |

**추천은 B를 뼈대로, 카드를 펼치면 A의 편집판, 등록 직전에 D의 질문이다.**
셋은 배타적이지 않다.

### 왜 B인가

- 전부 잘 읽힌 날에 손이 **한 번만** 간다. 카카오톡 원본은 거의 다 제대로 읽히니
  그게 대부분의 날이고, 그 경우를 빠르게 만드는 것이 여러 장을 한 번에 올리는 이유다.
- **아이폰에서는 이 화면이 유일한 다건 경로다.** 사진첩을 훑을 수 없기 때문이다
  (`client/src/utils/gallery.js`의 `isGalleryScanSupported` 참고). B로 가면
  안드로이드의 자동 찾기 결과와 아이폰의 직접 올리기가 똑같이 생긴 화면으로 모인다.
  배울 것도 만들 것도 하나가 된다.

### 이미 있는 것

묶는 규칙은 새로 만들 것이 없다. `client/src/utils/gallery.js`에 그대로 있다.

- 같은 바코드 번호면 한 건 (`seenCodes`)
- 원본(카카오톡·다운로드)이 있으면 캡처는 빼고 (`isOriginal`)
- 바코드가 사진 폭의 25%에 못 미치면 후보에서 제외 (`MIN_BARCODE_COVERAGE`)

### 할 일

- [x] 시안 확정 — B를 뼈대로, 카드를 펼치면 편집판, 빠진 칸만 묻기
- [x] `+`에서 사진 여러 장 받기 — 이미 여러 장을 받고 있었다. 달라진 것은 그 사진들이
      서로 다른 기프티콘일 때다. 예전에는 "다른 기프티콘인데 새로 시작할까요?"라고
      물었는데, 다섯 장을 골라 온 사람에게는 네 번 묻고 네 번 버리는 셈이었다.
- [x] 받은 사진을 위 규칙으로 묶기 — `collect()`에서 사진 한 장을 가져오는 걸음만
      밖으로 뺐다(`read`). 사진첩은 네이티브에게 묻고 직접 고른 사진은 브라우저에서
      줄인다. 묶고 고르는 판단은 한 벌 그대로다.
- [x] 묶은 결과를 확인시키는 화면 — 훑기 화면을 그대로 쓴다(`files` prop). 새 화면을
      만들지 않았다. 아이폰에서는 이 길이 유일한 다건 경로가 되므로, 둘이 다르게
      생기면 배울 것도 고칠 것도 두 벌이 된다.
- [x] 일괄 등록 — `registerAll`과 미리 올려두기를 그대로 쓴다
- [x] 카드를 펼쳐 빠진 칸 채우기 + 읽다가 막힌 건 다시 읽기
- [x] **여러 장을 넣을 수 있다는 걸 화면이 말하게 하기** — 기능은 위에서 다 됐는데
      화면 어디에도 흔적이 없었다. `+`를 누르면 한 건짜리 폼이 뜨니 한 장 넣는 자리로
      읽히는 게 당연하고, 해보기 전에는 알 수가 없다. 그리고 대부분은 안 해본다.
      사진이 아직 없는 등록 화면에서만 사진 자리를 세 칸으로 벌리고, 아래에 한 줄을
      붙였다 — "여러 장을 고르면 기프티콘별로 나눠 담아요"
      (`client/src/components/UploadSheet.jsx`의 `showBulkHint`).
      칸이 여럿인 건 글을 안 읽어도 보이고, 한 줄이 그게 무슨 뜻인지 마무리한다.
      한 장이라도 붙으면 평소 격자로 돌아간다 — 그때는 이미 아는 사람이다.
      수정 화면에서는 띄우지 않는다. 거기서 고른 사진은 보고 있는 그 한 건에 붙으니
      "나눠 담아요"가 거짓말이 된다.
      입구를 둘로 나누는 안(한 장 고르기 / 여러 장 고르기)은 버렸다. 발견성은 제일
      좋은데 같은 일을 하는 버튼이 둘 생기고, 화면에 없는 구분을 화면에 만들어두면
      나중에 둘이 갈라진다.

### 아직 안 한 것

- [ ] **아이폰에서 실제로 되는지.** 로직은 브라우저에서 도는 것뿐이라 될 것으로 보지만,
      아이폰 빌드 자체가 아직 없어서 확인할 방법이 없다.
- [ ] **한 번에 몇 장까지 받을지 정하기.** 지금은 상한이 없다. 사진첩 훑기는 200장으로
      묶어뒀는데(`MAX_IMAGES`), 이쪽은 사람이 손으로 고르는 것이라 그렇게 많이 고를 일이
      없다고 보고 두었다. 서른 장쯤 골랐을 때 어떻게 되는지는 재보지 않았다.
- [ ] **묶기를 두 번 돈다.** 등록 창이 "몇 건인가"를 보려고 얕게 한 번 읽고, 넘겨받은
      화면이 정밀하게 다시 읽는다. 두 장이면 눈에 안 띄지만 열 장이면 티가 날 수 있다.
      한 번 읽은 결과를 넘기는 쪽이 맞는데, 두 화면이 필요로 하는 모양이 달라서 미뤘다.

---

## 3. 알림 기록을 지우는 손이 없다

`purge_old_activities()`는 있는데 부르는 곳이 없다 (`supabase/schema.sql`).
30일 지난 기록을 지우는 함수인데, 아무도 부르지 않으면 `activities`가 계속 자란다.

- [x] 예약 SQL을 써뒀다 — **`supabase/purge-cron.sql`** 을 Supabase SQL 편집기에서
      한 번 돌리면 된다(태수님). 채울 값이 없다. 함수가 데이터베이스 안에 있어서
      밖으로 나가지 않기 때문이다 — `push-cron.sql`처럼 프로젝트ref나 비밀값을 넣을
      일이 없다.
- [ ] 걸고 나서 `select jobname, schedule from cron.job;`으로 확인
- [ ] 이미 걸려 있었다면 이 파일은 다시 걸어줄 뿐이라 그냥 돌려도 된다

앱 화면에는 이미 "30일이 지난 알림은 자동으로 지워져요"라고 적혀 있다
(`client/src/components/ActivitySheet.jsx`). 이걸 걸어야 그 말이 사실이 된다.

---

## 4. 앱을 열었을 때 맨 위에 무엇을 띄울까

자리는 하나인데 띄우고 싶은 것이 셋이었다 — 환영 인사, 운영 공지, 주변 매장 안내.
겹치면 목록이 두세 줄씩 밀린다.

**정한 것: 그 자리는 주변 매장 안내가 갖는다.** 기프티콘을 못 쓰고 버리는 진짜 이유는
기한을 몰라서가 아니라 매장 앞을 지나가면서도 가진 걸 떠올리지 못해서라, 이 앱에서
가장 힘이 센 자리다. 나머지 둘은 다른 길을 찾는다.

### 4-1. 환영 인사 — 공지 시스템에 얹지 않는다

지금은 운영 공지로 띄우고 있는데, 공지는 성격이 "닫을 때까지 남는 것"이다
(`client/src/components/NoticeBanner.jsx`). 거기에 "처음 한 번만"을 섞으면 공지 규칙이
거짓말이 된다.

- [x] 가입하고 처음 들어왔을 때만 그 자리에 뜨는 별도의 띠
      (`client/src/components/WelcomeBanner.jsx`)
- [x] X로 닫거나, 앱을 껐다 켜면 다시 안 뜬다 — **띄우는 순간에 봤다고 적는다.**
      X를 눌러야만 적으면 안 닫고 앱을 끈 사람에게 내일 또 뜨고, 그러면 닫기 버튼이
      있으나 마나가 된다.
- [x] 기존 환영 공지는 지우지 않고 그대로 둔다

**기기 기록만 보지 않는다.** 본 적 있는지만 보면, 오래 쓰던 사람이 폰을 바꿔 새로
깔았을 때 "환영해요"가 뜬다. 그 사람에게는 처음이 아니다. 서버에 있는 가입 시각
(`members.created_at`)을 같이 보고 **가입 7일 안**일 때만 띄운다. 하루가 아니라
일주일인 이유는, 초대는 받은 날 수락하고 앱은 주말에 여는 일이 흔해서다.

**문구** — "＋를 누르세요"는 적지 않았다. 기프티콘이 없는 목록이 이미 그 말을 하고 있고
(`client/src/components/GifticonList.jsx:18`), 같은 말이 두 줄이면 둘 다 안 읽힌다.
대신 눌러보기 전에는 알 수 없는 것을 적는다 — 이게 나 혼자 쓰는 앱이 아니라는 것.

> 모아콘에 오신 걸 환영해요.
> 여기 올린 기프티콘은 가족 모두가 함께 봐요. 사진만 올리면 상품명과 기한은 알아서 채워드려요.

### 4-2. 운영 공지 — 종 안으로

**푸시는 쓰지 않는다.** 한 번 그렇게 하려다 접었다. 공지를 종류별로 늘어놓아 보니
미리 알려야 하는 것이 거의 없었다 — 새벽 점검은 그 시간에 다들 자고, 장애는 미리 알 수
없고, 기능 안내는 애초에 푸시할 일이 아니다. 전부 앱을 열었을 때 알면 되는 것들이다.
이런 것으로 푸시가 몇 번 가면 정작 **기한 임박 푸시까지 같이 꺼진다.**

**맨 위 띠도 쓰지 않는다.** 그 자리는 주변 매장 안내가 갖는다.

시안 — <https://claude.ai/code/artifact/323a4429-0fc2-4115-b56f-2363867bbf75>

#### 중요공지는 한 종류가 아니다

운영하는 쪽에서 꼭 전하고 싶은 것이 다 여기 든다. 사용자에게는 서로 다른 일이다.

| | 모르면 | 급한가 | 대상 |
| --- | --- | --- | --- |
| 점검·장애 | **헛수고를 한다** — 등록을 세 번 눌러보고 앱을 탓한다 | 급함 | 등록하려는 사람만 |
| 유료화·정책·개선 보고 | **손해를 본다** — 돈이 걸린 일이 된다 | 안 급함 | 모두 |

#### 정한 것

**기준은 하나다 — 계산대 앞을 막지 않는다.** 이 앱을 여는 순간 중에 제일 급한 것이
계산대 앞이다. 뒷사람이 기다리는데 무엇이든 앞을 막으면 그건 방해다. 앱을 열자마자
막아서는 팝업을 안 만드는 이유가 이것이다.

- [x] **점검·장애는 등록을 누르는 순간 그 자리에서 알린다.** 앱을 열 때는 아무 말도
      하지 않는다. 바코드를 띄우러 온 사람은 공지가 있는 줄도 모르고 잘 쓰고 나가는데,
      그게 가장 좋은 결과다. 바코드는 저장된 것이라 점검 중에도 그대로 된다.

      **점검 공지인지는 `notices.blocks_upload`로 가른다**(`supabase/schema.sql:948`).
      `is_important`와 나눈 이유는, 유료화 안내도 중요 공지인데 그게 등록을 막으면 안 되기
      때문이다. "꼭 봐야 하는가"와 "지금 그 기능이 되는가"는 다른 이야기다.
- [x] **모든 중요공지는 종 옆에 말풍선으로 알린다.** 6초 뒤 사라진다(처음엔 3초였는데,
      앱을 켜면 목록부터 읽느라 눈이 아래에 있어서 못 보고 지나갔다. 하루 한 번뿐이라
      놓치면 그날은 끝이다).

      **모양은 세 번 고쳤다.** 처음엔 검은 상자가 종 아래에 그냥 떴는데 "왜 여기 이게
      떠 있지"가 남았다. 빠진 것이 셋이었다 — 무엇에 대한 말인지 안 붙어 있고(꼬리가
      없어 종을 안 가리킨다), 눌러도 아무 일이 없고, 아무 예고 없이 나타난다.

      지금은 **꼬리로 종을 가리키고, 뜰 때 종이 한 번 까딱하고, 누르면 알림이 열린다.**
      움직이는 것이 말하는 것이라 종이 먼저 움직이면 눈이 그리로 간다. "보기" 같은
      안내는 안 붙였다 — 이걸 본 사람은 어차피 종을 누른다. 말풍선을 눌러도 같은 곳으로
      가는 건 덤이지 약속이 아니다.

      색은 검정에서 앱의 보라로 낮췄다. 검정은 이 앱에서 제일 센 색인데 이건 알림이 아니라
      귀띔이고, 정작 급한 것(빨간 배지)과도 안 겹쳐야 한다.

      시안 — <https://claude.ai/code/artifact/a5cef7ba-349d-4d07-8d6e-9d0815e4237d> 문구는
      **"중요 안내가 있어요"** — 점검이든 유료화든 같은 말로 덮는다.
- [x] **하루에 한 번만.** 읽으면 그 뒤로 안 뜨고, 안 읽으면 다음 날 다시 한 번.
      앱을 하루에 네다섯 번 열 일이 없고, 유료화 같은 것은 한 달 전부터 공지하므로
      하루 놓쳐도 된다. 급한 점검은 위의 등록 자리가 이미 잡아준다.
- [x] **종을 열면 중요공지를 맨 위에 고정한다. 최대 두 개.** 확성기와 색으로 가족 활동과
      가른다. 스크롤해도 안 사라진다.

      하나로 할까 했는데 둘로 늘렸다. 성격이 다른 둘이 겹칠 때가 있다 — 점검 중인데
      유료화 안내도 떠 있는 식이다. 셋이 되면 정작 활동이 안 보이므로 거기서 끊는다.
      셋 이상이면 최근 둘만 고정하고 나머지는 목록에 섞인다.
- [x] **게시일자는 적지 않는다.** 노출기한이 하루 안쪽일 때만 "오늘 4시까지"처럼
      남은 시간을 적는다.

      노출기한을 그대로 적으면 오해를 산다. 그건 운영하는 쪽 사정이지 사용자가 알고
      싶은 날짜가 아니다. "유료화 안내 · 2026.09.30까지"라고 적혀 있으면 읽는 사람은
      **9월 30일에 유료화되나** 하고 읽는다. 시행일은 본문에 적는다.
- [x] 데이터가 다르다는 것에 주의 — 종에 쌓이는 것은 `activities`(가족 단위)이고 공지는
      전체 대상이다. 한 테이블에 섞지 말고 화면에서 합쳐 보여준다.
- [x] 맨 위 띠에서 공지를 뺐다. `NoticeBanner.jsx`는 지웠다. `yielded`는 남겼다 —
      비켜설 상대가 공지에서 환영 인사로 바뀌었을 뿐 자리 다툼 자체는 그대로다.

#### 아직 안 한 일 — SQL 한 번 (태수님)

**`supabase/schema.sql`을 Supabase SQL 편집기에서 한 번 돌려야 한다.** 이걸 안 하면
공지는 종에 안 쌓이고(읽음을 적을 표가 없다) 점검 표시도 늘 꺼진 것으로 읽힌다.
화면은 안 죽는다 — 표가 없으면 "안 읽었다"로 보고 그냥 넘어가게 해뒀다.
두 가지가 들어간다.

- `notices.blocks_upload` — 이 공지가 뜬 동안 등록을 막을지. Supabase 표 편집기에서
  공지를 쓸 때 체크하면 된다
- `notice_reads` 테이블 — 누가 어느 공지를 읽었는지

#### 나머지 규칙

- [x] **읽었다 = 종을 연 것.** 공지가 맨 위에 고정돼 있어 열면 무조건 보인다. 한 번 더
      누르게 하면 일만 는다. 읽음은 공지별로 적는다.
- [x] **배지는 공지도 함께 센다.** 색을 다르게 하려다 접었다 — 색이 갈리면 "이 색이 무슨
      뜻이지"를 새로 배워야 하는데, 사용자에게는 둘 다 그냥 **안내**다. 열 개면 열 개다.
- [x] **읽음은 계정에 적는다.** `notice_reads` 테이블을 새로 뒀다
      (`supabase/schema.sql:973`). 기기에 적으면 배지가 두 기준으로 세게 된다 — 활동
      읽음은 서버에 있는데 공지만 기기에 있으면, 한 폰에서 읽고 다른 폰에서 보면 숫자가
      다르다. 말풍선의 "오늘 봤나"도 같은 값을 보면 되므로 서버를 더 다녀올 일은 없다.
- [x] **점검 중에는 등록을 막는다.** `+`와 갤러리 훑기 둘 다 막는다. 자동 훑기(앱을
      열자마자 뜨는 것)도 같이 막는다 — 버튼만 막아두면 자동으로 켠 사람은 점검 중에도
      훑기 창을 보게 된다. "안 된다"고 해놓고 누를 수 있으면 눌러보고 실패한다.
      두 번 실망시키는 셈이다.
- [x] **어느 기능이 막히는지는 따로 적지 않는다.** 공지 본문에 쓴다. 지금 막힐 만한 것이
      등록 하나뿐이라 굳이 나눌 이유가 없다 — 모델을 부르는 자리가 거기뿐이고 바코드·목록·
      매장은 점검과 무관하게 된다.
- [x] 환영 띠와 말풍선이 같은 날 겹쳐도 그냥 둔다. 하나는 띠고 하나는 종 위라 자리가
      안 겹친다.

### 4-3. 주변 매장 안내 — 지금 이렇게 돌고 있다

읽어보니 두 가지가 생각과 달랐다.

| | 지금 |
| --- | --- |
| 언제 찾나 | **앱을 열고 목록이 처음 채워졌을 때 딱 한 번** (`ranRef`) |
| 켠 채로 이동하면 | **아무 일도 안 일어난다.** 다시 안 찾는다 |
| X로 닫으면 | `sessionStorage`라 **앱을 껐다 켜면 다시 뜬다** |

캐시에 10분·300m 규칙이 있지만(`CACHE_TTL_MS`, `CACHE_MOVE_M`), 다시 찾지를 않으니
그 규칙이 쓰일 일이 앱을 껐다 켤 때뿐이다.

- [x] **앱이 다시 앞으로 올 때 한 번 더 찾는다.** `@capacitor/app`의 `appStateChange`를
      쓰면 된다(이미 깔려 있다).

      홈 버튼으로 뒤로 보냈다 다시 열 때, 화면을 껐다 켤 때, 다른 앱 쓰다 돌아올 때가
      여기 든다. 앱을 아주 꺼버렸다 켜는 경우는 지금도 새로 찾는다 — 컴포넌트가 새로
      뜨기 때문이다. 문제는 **사람들이 앱을 잘 안 끈다**는 것이다. 홈을 누르고 주머니에
      넣는다. 그러고 500m를 걸어가서 앱을 다시 봐도 아까 그 내용이 그대로 있다.

      캐시(10분·300m)가 이미 있어 호출은 안 샌다. 켜둔 채로 주기적으로 도는 것은 하지
      않는다 — 배터리와 검색 한도를 쓰는데, 앱을 켜둔 채 걷는 일은 드물다.
- [x] **열 때마다 위치를 새로 잡는다.** 여의도에서 앱을 켰는데 몇 시간 전 서울숲에서
      잡아둔 좌표로 매장을 찾고 있었다. 검색은 멀쩡했다 — 카카오에 `sort=distance`로
      묻고 500m 안만 띄운다. 앱이 자기 위치를 잘못 알고 있었다.

      **처음 지목한 범인은 틀렸다.** `getFreshPosition`의 `maximumAge: Infinity`를 짚었는데,
      그 코드는 애초에 돌지도 않고 있었다. 띠는 위치를 잡기 전에
      `navigator.permissions.query({name:'geolocation'})`가 `granted`인지 보는데,
      **안드로이드 웹뷰는 앱 권한이 있어도 `prompt`를 돌려준다** — 크롬과 달리 웹뷰에는
      사이트별 권한 설정이 없고 허용은 앱 권한으로 처리되기 때문이다. 그래서 띠는 위치를
      한 번도 안 잡았고, **매장 찾기가 적어둔 옛 좌표만** 계속 읽고 있었다. 매장 찾기는
      권한 API를 안 보고 바로 잡으므로 서울숲에서 한 번 쓴 것이 24시간 캐시에 남았고,
      여의도에서도 그게 떴다.

      그래서 근거를 하나 더 뒀다 — **적어둔 위치가 있으면 권한을 준 것으로 본다**
      (`hasSavedPosition`). 매장 찾기가 한 번은 잡았다는 뜻이라, 다시 잡아도 권한 창이
      뜨지 않는다. 거절(`denied`)이 확실할 때만 안 잡는다.

      캐시를 30분으로 줄이자 그 옛 좌표마저 안 쓰여서 **띠가 아예 안 떴다.** 두 고침이
      같이 가야 한다.

      `maximumAge: Infinity`도 함께 고쳤다(`client/src/utils/geolocation.js`).
      "나이 상관없이 기기가 마지막으로 잡아둔 것을 달라"는 뜻이라 기다릴 일이 없었는데,
      이동하는 내내 화면을 꺼두면 기기가 새로 잡을 일이 없어서 옛 기억이 계속 이긴다.
      앱을 껐다 켜도 그대로였다. 1분으로 조였다.

      띠가 받아들이는 지난 위치도 하루에서 **30분**으로 줄였다. 매장 찾기는 사용자가
      직접 눌러 지도까지 보는 자리라 하루를 그대로 두지만, 이 띠는 묻지도 않았는데 먼저
      말을 거는 자리다. 틀린 채로 말을 걸면 안 하느니만 못하다.
- [x] **위치를 못 잡으면 왜 아무것도 없는지 말해준다.** 지하·실내에서는 위치가 안 잡히는데,
      그때 띠가 그냥 사라지면 "이 기능이 고장 났나" 하게 된다. 그리고 지하는 자주 간다.

      > 지금은 위치를 확인할 수 없어요. 지하나 실내에서는 안 잡힐 수 있어요.

      **권한을 안 준 경우에는 안 띄운다.** 그건 장소 탓이 아니고, 이 앱은 일부러 위치
      권한을 조르지 않기로 했다 — 여기서 그 이야기를 꺼내면 결국 조르는 것이 된다.
      잡으려다 실패한 경우(`POSITION_UNAVAILABLE`·`TIMEOUT`)만 해당한다.

      **X는 안 붙였다.** 닫으면 그날 하루 안 뜨는 규칙에 걸리는데, 지하에서 한 번 닫았다고
      밖에 나온 뒤 진짜 안내까지 못 받으면 손해가 훨씬 크다. 위치가 잡히면 저절로 사라진다.
- [x] **위치를 아직 안 준 사람에게는 이 자리에서 먼저 묻는다.**

      > 📍 위치 권한을 켜면 근처에서 쓸 수 있는 기프티콘을 알려드려요. **[켜기]**

      안 물어보는 쪽으로 두었더니 **아무한테도 안 물었다.** 매장 찾기를 먼저 써봐야 띠가
      도는데 그건 카드 메뉴 안에 있어서, 이 앱에서 제일 힘이 센 기능이 대부분의 사람에게
      죽어 있었다.

      그렇다고 앱을 열자마자 시스템 창을 띄우지는 않는다. **안드로이드 위치 권한은
      되돌리기가 어렵다** — 두 번째 거절은 "다시 묻지 않음"이 되어 시스템 설정에 들어가야
      풀린다. 앱이 뭘 하는지도 모르는 상태에서 물으면 반사적으로 거절하고, 그러면 띠도
      매장 찾기도 영영 막힌다. **시스템 창이 첫 질문이 되면 안 된다.**

      그래서 우리가 먼저 묻는다. 이 자리는 어차피 비어 있고, **누르기 전에는 시스템 창이
      안 뜨므로 무시해도 잃는 것이 없다.** "위치를 허용하시겠습니까?"보다 "근처에서 쓸 수
      있는 걸 알려드릴까요?"가 훨씬 승낙받기 쉽다 — 무엇을 위해 묻는지가 그 자리에 적혀
      있어서다.

      기프티콘이 하나라도 있을 때만 묻는다. 거절하면 그날은 다시 묻지 않고 다음 날 한 번
      더 묻는다. 매장 찾기에서 이미 허락받았으면 묻지 않고 바로 띠가 돈다.
- [x] **X를 누르면 그날 하루 안 뜬다.** 움직였어도 그날은 안 띄운다.

      한때 "300m 넘게 움직였으면 내용이 달라졌으니 다시 띄우자"고 했다가 접었다.
      그건 앱 입장의 논리다. 겨우 한 줄짜리 띠에 X를 눌렀다는 건 안 보겠다는 뜻이고,
      거기서 앱이 이기려 들면 안 된다. 다음 날 다시 띄우는 것으로 충분하다.
      날짜 하나만 기억하면 되니 만들기도 더 단순하다.

---

## 출시 이후에 할 것

지금 만들지 않는다. 출시하고 몇 달은 지나야 필요해지는 것들이라, 그전에 만들면
쓰이지도 않는 것을 안고 나가게 된다.

### 오래 묵은 것 정리 안내

넣어두고 잊은 기프티콘을 다시 눈에 띄게 하는 일. 시안을 그려뒀다 —
<https://claude.ai/code/artifact/d27d1bb9-2e35-43ea-82f8-e6b746d8711c>

필요해지는 시점이 **출시 후 최소 3개월**이다. 그전에는 묵은 것 자체가 없다.

- [ ] 하단 띠 → 정리 화면. 여러 개를 한 번에 사용완료
- [ ] 3 / 6 / 12개월 세 번까지만. 기프티콘 단위로 세고, 동시에 걸린 것은 한 띠로 묶는다
- [ ] 한 번 확인하면 다음 차수까지 안 뜬다. X도 같게 둔다
- [ ] **정해야 할 것** — "오래됨"의 기준이 등록일인가 마지막으로 바코드를 연 날인가.
      후자가 정확한데 그 기록이 지금 없어서 새로 만들어야 한다. 등록일로만 세면
      어제 확인한 것도 묵은 것이 된다
- [ ] **정해야 할 것** — 기한이 넉넉한 것(D-300)도 대상인가. 기한 없는 것과 지난 것만
      대상으로 하는 쪽이 말이 된다
