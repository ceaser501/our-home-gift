# Include your project-specific ignores in this file
# Read about how to use .gitignore: https://help.github.com/articles/ignoring-files
# Useful .gitignore templates: https://github.com/github/gitignore
node_modules
dist
.cache
.idea
*.iml

# 앱 서명 키. 저장소에 들어가면 안 된다 — 이 파일과 비밀번호를 가진 사람은 누구나
# "모아콘"이라는 이름으로 앱을 만들어 배포할 수 있다. 저장소 밖에 따로 보관한다.
# (빌드할 때는 GitHub 시크릿에서 꺼내 쓴다. docs/app-release.md 참고)
*.keystore
*.jks

# Supabase CLI가 작업하면서 만드는 임시 폴더. 이 컴퓨터에서만 뜻이 있다.
supabase/.temp/

# 맥이 파일을 복사하거나 동기화할 때 만드는 중복본("config 2.xml" 같은 것).
#
# 그냥 지저분한 것으로 끝나지 않는다. 안드로이드 리소스 폴더에 생기면 빌드가 깨진다 —
# res/ 아래 파일 이름에는 띄어쓰기를 쓸 수 없어서, 커밋되는 순간 리소스 컴파일이
# 실패한다. 눈에 잘 안 띄는 자리에서 나는 실패라 원인을 찾는 데 오래 걸린다.
* [0-9].*
*.DS_Store

# 저장소 안에 또 받아둔 저장소. 실수로 여기에 clone한 것이지 우리 소스가 아니다.
/our-home-gift/

# 아직 정해지지 않은 것을 적어두는 메모. 저장소에 올리지 않는다 —
# 추정과 확인된 사실이 섞여 있어서, 공개된 문서로 두면 나중에 사실처럼 읽힌다.
# 결정이 끝난 내용만 docs/의 정식 문서로 옮긴다.
*.local.md