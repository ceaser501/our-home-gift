# 모아콘 작업 규칙

## 답변에서 파일을 가리킬 때

파일을 언급할 때는 저장소 기준 전체 경로로 적는다. 터미널에서 그대로 눌러 열 수 있다.
줄까지 짚을 때는 `경로:줄번호` 형식을 쓴다.

- `supabase/schema.sql` (O)
- `supabase/functions/analyze-gifticon/index.ts:64` (O)
- "schema.sql 다시 실행해주세요" (X — 어느 파일인지 눌러서 열 수 없다)

특히 직접 실행하거나 배포해야 하는 파일(SQL, Edge Function)은 반드시 이렇게 적는다.
그게 사용자가 다음에 할 일이라서, 찾아 헤매지 않고 바로 열 수 있어야 한다.

## SQL 파일은 링크로 준다

**돌려야 하는 SQL은 경로만 적지 말고 raw 링크를 함께 준다.** 태수님은 그걸 열어 전체
선택해 Supabase SQL Editor에 붙여넣는다. 경로만 적으면 저장소를 찾아 들어가 파일을
열고 다시 raw를 눌러야 한다 — 그 세 단계가 매번 반복된다.

```
https://raw.githubusercontent.com/ceaser501/our-home-gift/<지금 작업 브랜치>/<경로>
```

지금 작업 브랜치의 링크를 준다. main은 대개 뒤처져 있어서 방금 고친 것이 없다.

## 배포

작업은 브랜치에서 한다. **"배포하자"는 브랜치에서 태그를 딴다는 뜻이다.**
main 머지는 태수님이 따로 말할 때만 한다 — 묻지 않고 하지 않는다.

```
npm run release             # 앱 (APK). 태그를 달면 빌드가 돈다
git push origin HEAD:main   # 웹. "main에 반영하자"고 할 때만
```

둘을 갈라 둔 이유는 나가는 속도가 달라서다. 앱은 손에 쥐고 고쳐가며 몇 번이고 태그를
따는 것이고, 웹은 다 본 뒤에 한 번 내보내는 것이다.

**대신 둘이 벌어진다.** 웹은 main에 밀 때만 나가므로(`.github/workflows/deploy-pages.yml`)
브랜치에서 태그만 계속 따면 앱은 새것, 웹은 옛것이 된다. 한 번 닷새를 그렇게 보냈고
그때는 모르고 그랬다. 이제는 알고 하는 것이니 괜찮지만, **웹이 몇 커밋 뒤처져 있는지는
배포할 때마다 답변에 적는다.** 모르고 벌어지는 것과 알고 벌어지는 것은 다르다.

**태그 밀기는 이 환경에서 자주 403으로 막힌다.** 프록시가 막는 것이고 되는 날도 있어서
규칙을 세울 수가 없다(한때 "main에 없는 커밋이라 그렇다"고 적었는데, 그 뒤 main이 최신인
상태에서도 막혔다). 한 번 해보고 안 되면 `npm run release` 한 줄을 태수님께 드린다 —
붙들고 재시도하지 않는다. 만들어둔 로컬 태그는 지우고 넘긴다.

무엇을 쳤는지는 답변에 그대로 적는다. 배포는 되돌리기가 비싸서, 무엇이 나갔는지
태수님도 알고 있어야 한다.

## 앱 버전 태그

**다음 버전 번호를 세지 않는다.** `scripts/release.sh`가 센다.

```
npm run release          # 0.0.34 다음이면 0.0.35
npm run release:dry      # 무엇을 할지만 보여준다
```

번호를 손으로 세다가 여러 번 틀렸다. 원인은 늘 같았다 — 손에 있는 태그 목록이
낡은 것이었다. 원격에는 35가 있는데 이쪽에는 33까지만 있으면 아무리 세어봐도
34가 나오고, 밀면 "이미 있다"는 말을 듣는다. 스크립트는 늘 `git fetch --tags`를
먼저 하고 원격에 있는 가장 큰 번호에서 하나를 올린다.

그래서 답변에 `git tag app-v0.0.35` 같은 명령을 적지 않는다. 그 숫자가 맞는지
확인할 방법이 이쪽에 없다. `npm run release` 한 줄만 적는다.
